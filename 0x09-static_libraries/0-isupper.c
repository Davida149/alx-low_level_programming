#include "main.h"

/**
 *_isupper - A function that prints whether a letter is upper or lowercase
 *@c: is the int to check
 *Return: returns 1 if upper and 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);


}
